---
meta:
  environment: NAME
  dns_root: microbosh
  network_name: elasticsearch1
  security_groups:
    - elasticsearch

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: elasticsearch
    version: latest
